effect {
    Name: "Boost",
    Params: {
        amount: Number,
        duration: Number
    },
    Action: (targets, context) =>
        for target in targets {
            target.Power += amount;
            context.Board.Shuffle();
            Context.Find((unit) => unit.Power < 5);
        }
}

";